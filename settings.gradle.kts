pluginManagement {
    repositories {
        google()  // Standard declaration, NO content filtering
        mavenCentral()
        gradlePluginPortal()
    }
}
@Suppress("UnstableApiUsage")
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "aiKaloriaSzamlalo"
include(":app")